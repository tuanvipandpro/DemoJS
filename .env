# Local config for Postgres (pgvector) used by docker compose
# Copy to .env or run: docker compose --env-file ./env.example up -d

POSTGRES_DB=XXXXXXXXX
POSTGRES_USER=XXXXXXXXXXX
POSTGRES_PASSWORD=XXXXXXXXXXX

# Host path for Postgres data (Windows path allowed). Default to D:/Mount/InsightTestAI
PGDATA_PATH=XXXXXXXXXX

VITE_GITHUB_CLIENT_ID=XXXXXX
VITE_GITHUB_REDIRECT_URI=XXXXXXXXXXX